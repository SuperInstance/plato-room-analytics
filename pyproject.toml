[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-room-analytics"
version = "0.1.0"
description = "Room usage analytics — metrics, trends, agent activity tracking"
license = {text = "MIT"}
requires-python = ">=3.8"
